import { NextResponse } from "next/server"; import { eq } from "drizzle-orm"; import { db } from "@/db/client"; import { auditLogs, users } from "@/db/schema"; import { requireSession, jsonError, ApiError } from "@/lib/api"; import { registerSchema } from "@/lib/validation"; import { isAdminTelegramId } from "@/lib/telegram";
export async function POST(request: Request) { try { const session = await requireSession(); const parsed = registerSchema.safeParse(await request.json()); if (!parsed.success) throw new ApiError(400, "Проверьте имя и подразделение", "VALIDATION_ERROR"); const role = isAdminTelegramId(session.id) ? "admin" : "participant"; const [existing] = await db.select().from(users).where(eq(users.telegramId, session.id)).limit(1); if (existing) return NextResponse.json({ user: existing, created: false }); const [user] = await db.insert(users).values({ telegramId: session.id, telegramUsername: session.username, firstName: session.firstName, lastName: session.lastName, displayName: parsed.data.displayName, department: parsed.data.department || null, role }).returning(); await db.insert(auditLogs).values({ actorTelegramId: session.id, action: "user.registered", entityType: "user", entityId: String(user.id) }); return NextResponse.json({ user, created: true }, { status: 201 }); } catch (error) { return jsonError(error, "register"); } }
